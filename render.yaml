services:
  - type: web
    name: chaotic-cryptography
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run chaotic_crypto_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0